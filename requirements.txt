# Pin versions when you start training (torch+cuda must match your GPU stack).
torch>=2.2.0
torchvision>=0.17.0
numpy>=1.26.0
PyYAML>=6.0.1
Pillow>=10.0.0
typer>=0.12.0
rich>=13.0.0

# Diffusion training (also: pip install -e ".[train]")
diffusers>=0.27.0
accelerate>=0.28.0
